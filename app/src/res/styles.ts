/**
 * common
 * common styles
 *
 * @format
 */

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  flexOne: {
    flex: 1,
  },
  flexDirectionRow: {
    flexDirection: 'row',
  },
});

export default styles;
